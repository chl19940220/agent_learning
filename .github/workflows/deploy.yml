name: Deploy mdBook to GitHub Pages

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install mdBook
        run: |
          curl -sSL https://github.com/rust-lang/mdBook/releases/download/v0.4.52/mdbook-v0.4.52-x86_64-unknown-linux-gnu.tar.gz | tar -xz
          mv mdbook /usr/local/bin/

      - name: Install mdbook-katex
        run: |
          curl -sSL https://github.com/lzanini/mdbook-katex/releases/download/v0.9.4/mdbook-katex-v0.9.4-x86_64-unknown-linux-gnu.tar.gz | tar -xz
          mv mdbook-katex /usr/local/bin/

      - name: Build with mdBook
        run: |
          # Build Chinese version
          mdbook build

          # Build English version
          # mdbook v0.4.52 removed --config flag, use temporary book.toml swap instead
          cp book.toml book.toml.zh_bak
          cp book-en.toml book.toml
          mdbook build
          cp book.toml.zh_bak book.toml
          rm -f book.toml.zh_bak

          # Copy root index.html for language detection & redirection
          cp root-index.html ./book/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
